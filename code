import pandas as pd
df=pd.read_csv("dataset1.csv")
df = df[['review_format', 'stars', 'review_lang']]
dfm=df.dropna(subset=["review_format"])
dfm.replace({"unknown":None,"":None},inplace=True)
df_final=dfm.dropna()
print(df_final.columns)
print(df_final.head)